// import default text typography styles
import { Text } from '../../assets/styles/typography';

// define custom styles
const custom = {};

export default (props) => Object.assign({}, Text(props), custom);
