// import default heading typography styles
import { Heading } from '../../assets/styles/typography';

// define custom styles
const custom = {};

export default (props) => Object.assign({}, Heading(props), custom);
